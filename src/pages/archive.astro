---
export const prerender = false; // Not needed in 'server' mode

import siteData from "@/config/siteData.json";
import Button from "@components/Button/Button.astro";
import Categories from "@components/Gallery/Categories";
import BaseLayout from "@layouts/BaseLayout.astro";

const { publicGallery } = siteData;
---

<BaseLayout
	title="Galería de Fotos"
	description="Todas las fotos de nuestra boda !No te las pierdas! 💍📷"
>
	{
		!publicGallery && (
			<section class="site-container pt-36 text-center md:pt-44">
				<div class="mx-auto max-w-xl px-4 text-center">
					<h1 class="h1">Galería</h1>
					<p class="description mt-4">
						Próximamente las mejores fotos de la boda estarán disponibles en esta sección.
					</p>
					<div class="mt-8 flex justify-center">
						<Button variant="primary" href="/">
							Volver
						</Button>
					</div>
				</div>
			</section>
		)
	}
	{publicGallery && <Categories client:load />}
</BaseLayout>
